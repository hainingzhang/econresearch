# 分析说明

M0 报告州别—时期 FTE 均值及变化之差。M1 为 OLS `fte ~ treat + post + did`；若门店可以匹配，则采用餐厅聚类的不确定性估计。M2 加入预先设定的时不变控制变量：公司所有制和连锁店指示变量（省略一个连锁品牌）。地理虚拟变量虽可用，但由于其与本数据中的州处理指示变量共线，故不纳入模型。M3 以匹配门店的 FTE 变化对处理变量进行回归（等价于平衡面板 DID）。仅当下载的长格式数据提供替代就业结果时，M4 才会使用该结果。缺失的结果变量观测不作插补；每个模型均使用完整案例并报告其 N。
